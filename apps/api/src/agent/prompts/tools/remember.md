保存用户明确要求长期记住的信息，例如用户偏好、个人资料事实、目标、项目背景或长期指令。

仅在用户表达“记住”“以后按这个偏好”“我的信息是”等明确持久化意图时调用。不要保存临时对话内容、敏感凭据或未经用户确认的推测。

参数：
- content：需要持久化的文本。
- kind（可选）：`semantic`（默认）或其他分类标记（如 `preference`, `fact`, `goal`）。
