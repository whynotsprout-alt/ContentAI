"""Agent tool implementations.

Import the registry from :mod:`agent.tools.registry` explicitly.  Keeping this
package initializer side-effect free prevents search/research modules from
forming an import cycle while the registry is being built.
"""
