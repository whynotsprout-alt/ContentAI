# 删除清单（待执行）

> 说明：仅列入“已通过计划/当前无引用”或“已执行删除”的项。  
> 风险分级按当前改动影响范围给出。

## 已删除（已执行）
| 文件路径 | 删除原因 | 风险等级 | 说明 |
| --- | --- | --- | --- |
| `backend/src/agent/runtime/executor.py` | 与新运行时执行链路重复，已迁移到 runner/chat_stream 结构 | 中 | 删除前保留了功能等价入口，未见外部直接 import |
| `backend/src/agent/runtime/worker.py` | 已废弃线程 Worker 抽象，执行能力已内聚到 runner | 中 | 依赖路径需关注第三方 import；当前全局检索未见存量引用 |
| `backend/src/agent/runtime/worker_main.py` | 旧启动入口，与现有服务启动路径不再使用 | 低 | 删除后无 CLI 入口依赖 |
| `backend/src/core/config.py` | 配置结构已重构到 `backend/src/core/config/` 包 | 低 | 按新路径导入完成替换后清理 |

## 候选待确认（需要再走 `deprecated` 观察期）
| 文件路径 | 待确认原因 | 风险等级 | 观察建议 |
| --- | --- | --- | --- |
| 暂无 | 无新增高置信候选文件 | 低 | 当前仅保留明确已执行删除与本次新增重构文件，未发现新增高置信可删文件 |

## 风险说明
- 风险等级定义：`低` 为对外接口影响小；`中` 为内部模块重构后联动影响；`高` 为需停服/迁移窗口确认。
- 对无法稳定确认的文件，先不硬删，改用 `deprecated/` 标记观察期，避免一次性回滚风险。
